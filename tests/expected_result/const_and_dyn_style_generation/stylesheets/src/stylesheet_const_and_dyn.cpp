/*
 *  Automatically generated file: DO NOT MODIFY
 *  Generated by yaml_lv_style
 */

#include "const_and_dyn_style_generation/stylesheet_const_and_dyn.h"

/**
 * @brief Static and constant style definitions
 * Memory allocation for styles that are pre-calculated and stored in Flash/ROM.
 */
namespace {

// --- Definition for constant style: style1 ---
const lv_style_const_prop_t style1DefaultProps[] = {
    LV_STYLE_CONST_ALIGN(LV_ALIGN_TOP_LEFT),
    LV_STYLE_CONST_BG_COLOR(LV_COLOR_MAKE(67, 35, 67)),
    LV_STYLE_CONST_PROPS_END,
};
LV_STYLE_CONST_INIT(style1Default, style1DefaultProps);

const lv_style_const_prop_t style1HoveredProps[] = {
    LV_STYLE_CONST_BG_COLOR(LV_COLOR_MAKE(255, 255, 253)),
    LV_STYLE_CONST_PROPS_END,
};
LV_STYLE_CONST_INIT(style1Hovered, style1HoveredProps);

}  // namespace

/**
 * @brief Dynamic style declarations
 * Memory allocation for styles that require runtime initialization.
 */
static lv_style_t style2Default;
static lv_style_t style2Hovered;

/**
 * @brief Initializes all dynamic styles in the stylesheet
 * Sets properties at runtime for styles not marked as 'const'.
 */
void initConstAndDynStyleSheet() {
  // --- Definition for dynamic style: style2 ---
  lv_style_init(&style2Default);
  lv_style_set_align(&style2Default, LV_ALIGN_TOP_RIGHT);
  lv_style_set_bg_color(&style2Default, lv_color_make(18, 52, 86));

  lv_style_init(&style2Hovered);
  lv_style_set_bg_color(&style2Hovered, lv_color_make(0, 1, 2));
}

/**
 * @brief Applies a style to a given LVGL object
 * @param obj The LVGL object pointer
 * @param name The enum value of the style to apply
 */
void setConstAndDynStyle(lv_obj_t* obj, ConstAndDynStyle name) {
  switch (name) {
    case ConstAndDynStyle::Style1:
      lv_obj_add_style(obj, &style1Default, LV_STATE_DEFAULT);
      lv_obj_add_style(obj, &style1Hovered, LV_STATE_HOVERED);
      break;
    case ConstAndDynStyle::Style2:
      lv_obj_add_style(obj, &style2Default, LV_STATE_DEFAULT);
      lv_obj_add_style(obj, &style2Hovered, LV_STATE_HOVERED);
      break;
  }
}
