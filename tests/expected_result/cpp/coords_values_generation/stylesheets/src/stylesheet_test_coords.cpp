/*
 *  Automatically generated file
 *  DO NOT MODIFY: changes will be overwritten
 *  Generated by yaml_lv_style
 */

#include "coords_values_generation/stylesheet_test_coords.h"

lv_style_t TestCoordPctDefaultMain;
lv_style_t TestCoordPxDefaultMain;
lv_style_t TestSizeContentDefaultMain;
lv_style_t TestSizeLvglAliasDefaultMain;
lv_style_t TestSizePctDefaultMain;
lv_style_t TestSizePxDefaultMain;

/**
 * @brief Initializes all dynamic styles in the stylesheet
 * Sets properties at runtime for styles not marked as 'const'.
 */
void initTestCoordsStyleSheet() {
  // test_coord_pct
  lv_style_init(&TestCoordPctDefaultMain);
  lv_style_set_max_height(&TestCoordPctDefaultMain, LV_PCT(75));
  lv_style_set_max_width(&TestCoordPctDefaultMain, LV_PCT(100));
  lv_style_set_min_height(&TestCoordPctDefaultMain, LV_PCT(25));
  lv_style_set_min_width(&TestCoordPctDefaultMain, LV_PCT(50));
  lv_style_set_x(&TestCoordPctDefaultMain, LV_PCT(10));
  lv_style_set_y(&TestCoordPctDefaultMain, LV_PCT(20));

  // test_coord_px
  lv_style_init(&TestCoordPxDefaultMain);
  lv_style_set_max_height(&TestCoordPxDefaultMain, 300);
  lv_style_set_max_width(&TestCoordPxDefaultMain, 500);
  lv_style_set_min_height(&TestCoordPxDefaultMain, 50);
  lv_style_set_min_width(&TestCoordPxDefaultMain, 100);
  lv_style_set_x(&TestCoordPxDefaultMain, 10);
  lv_style_set_y(&TestCoordPxDefaultMain, 20);

  // test_size_content
  lv_style_init(&TestSizeContentDefaultMain);
  lv_style_set_height(&TestSizeContentDefaultMain, LV_SIZE_CONTENT);
  lv_style_set_width(&TestSizeContentDefaultMain, LV_SIZE_CONTENT);

  // test_size_lvgl_alias
  lv_style_init(&TestSizeLvglAliasDefaultMain);
  lv_style_set_height(&TestSizeLvglAliasDefaultMain, LV_SIZE_CONTENT);
  lv_style_set_width(&TestSizeLvglAliasDefaultMain, LV_SIZE_CONTENT);

  // test_size_pct
  lv_style_init(&TestSizePctDefaultMain);
  lv_style_set_height(&TestSizePctDefaultMain, LV_PCT(25));
  lv_style_set_width(&TestSizePctDefaultMain, LV_PCT(50));

  // test_size_px
  lv_style_init(&TestSizePxDefaultMain);
  lv_style_set_height(&TestSizePxDefaultMain, 50);
  lv_style_set_width(&TestSizePxDefaultMain, 100);
}

/**
 * @brief Applies a style to a given LVGL object
 * @param obj The LVGL object pointer
 * @param name The enum value of the style to apply
 */
void setTestCoordsStyle(lv_obj_t* obj, TestCoordsStyle name) {
  switch (name) {
    case TestCoordsStyle::TestCoordPct:
      lv_obj_add_style(obj, &TestCoordPctDefaultMain,
                       LV_STATE_DEFAULT | LV_PART_MAIN);
      break;
    case TestCoordsStyle::TestCoordPx:
      lv_obj_add_style(obj, &TestCoordPxDefaultMain,
                       LV_STATE_DEFAULT | LV_PART_MAIN);
      break;
    case TestCoordsStyle::TestSizeContent:
      lv_obj_add_style(obj, &TestSizeContentDefaultMain,
                       LV_STATE_DEFAULT | LV_PART_MAIN);
      break;
    case TestCoordsStyle::TestSizeLvglAlias:
      lv_obj_add_style(obj, &TestSizeLvglAliasDefaultMain,
                       LV_STATE_DEFAULT | LV_PART_MAIN);
      break;
    case TestCoordsStyle::TestSizePct:
      lv_obj_add_style(obj, &TestSizePctDefaultMain,
                       LV_STATE_DEFAULT | LV_PART_MAIN);
      break;
    case TestCoordsStyle::TestSizePx:
      lv_obj_add_style(obj, &TestSizePxDefaultMain,
                       LV_STATE_DEFAULT | LV_PART_MAIN);
      break;
  }
}
/**
 * @brief Gets a style from "test_coords" stylesheet
 * @param name The enum value of the style to apply
 * @param selector The LVGL part and state selector
 * @return The style pointer
 */
lv_style_t* getTestCoordsStyle(TestCoordsStyle name,
                               lv_style_selector_t selector) {
  switch (name) {
    case TestCoordsStyle::TestCoordPct:
      switch (selector) {
        case LV_STATE_DEFAULT | LV_PART_MAIN:
          return &TestCoordPctDefaultMain;
        default:
          return nullptr;
      }
    case TestCoordsStyle::TestCoordPx:
      switch (selector) {
        case LV_STATE_DEFAULT | LV_PART_MAIN:
          return &TestCoordPxDefaultMain;
        default:
          return nullptr;
      }
    case TestCoordsStyle::TestSizeContent:
      switch (selector) {
        case LV_STATE_DEFAULT | LV_PART_MAIN:
          return &TestSizeContentDefaultMain;
        default:
          return nullptr;
      }
    case TestCoordsStyle::TestSizeLvglAlias:
      switch (selector) {
        case LV_STATE_DEFAULT | LV_PART_MAIN:
          return &TestSizeLvglAliasDefaultMain;
        default:
          return nullptr;
      }
    case TestCoordsStyle::TestSizePct:
      switch (selector) {
        case LV_STATE_DEFAULT | LV_PART_MAIN:
          return &TestSizePctDefaultMain;
        default:
          return nullptr;
      }
    case TestCoordsStyle::TestSizePx:
      switch (selector) {
        case LV_STATE_DEFAULT | LV_PART_MAIN:
          return &TestSizePxDefaultMain;
        default:
          return nullptr;
      }
    default:
      return nullptr;
  }
}
